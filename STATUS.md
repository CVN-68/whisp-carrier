# STATUS — whisp-carrier の現在地

**この文書は揮発性。置換して使う。** 終わった項目は本文ごと
[MEASUREMENTS.md](MEASUREMENTS.md) へ移し、ここからは消す。
**追記して積まないこと** — それをやって「次の着手」が669行になり、
中身が「着手すべきものは無い」になった。

| 見たいもの | 場所 |
|-----------|------|
| 今どこにいるか・次に何をするか | **この文書** |
| なぜそう作ったか・踏むと壊れる罠・eval の使い方 | [HANDOVER.md](HANDOVER.md) |
| 測定結果・作業の記録・完了した項目 | [MEASUREMENTS.md](MEASUREMENTS.md) |
| 利用者向けの説明と**精度の数値の出どころ** | [README.md](README.md) |

**数値を引用するときは出典を確認すること。** 現行の基準値は下の表、
測定エントリごとの採点条件は [MEASUREMENTS.md](MEASUREMENTS.md) の各節直下にある。

---

## 現在地

**1.0.0 のビルドとパックが済んで、公開の操作だけが残っている。**
**公開済みは v0.9.2。1.0 の条件は 2026-08-27 に満たされた。**

### 引き継ぐならこの3点

**(1) 残っているのは公開の操作4つだけ。** `git push origin main`（**未 push 8コミット**）、
`git tag v1.0.0`、リリース作成とアセットのアップロード、
**アップロード後に digest と SHA-256 の一致を確認**。
**手元の成果物はできている。**

| | 値 |
|---|---|
| exe | `1.0.0` / 13,899,921 バイト / コード状態 `998798a` |
| アーカイブ | `dist/whisp-carrier-1.0.0.7z` / **1,055,056,170 バイト（1007 MiB）** |
| **SHA-256** | **`6494C638419B76FC6A43A103C7EF3309C4F295646570850E167C09791226AC29`** |
| チェックリスト | **16項目すべて通った**（[結果表](MEASUREMENTS.md#リリース前チェックリスト16項目の結果)） |

**ビルドは2回している。1回目は既定の実行が全部落ちた**
（`Path(None)`・[記録](MEASUREMENTS.md#100-の初回ビルドが既定の実行で全部落ちたpathnone2026-09-03)）。
**exe 限定で、スクリプト版では発火しない型。チェックリストの項目4が捕まえた。**

**(2) 精度の数値は差し替え済み。** 定型句フィルタが既定で有効になったので
**24分もの9本の基準が 16.1% → 15.5%** になり、
**残っていた6本（子供向け4本・一挙2本）も同じ世代で採り直した**
（[#28](MEASUREMENTS.md#28-定型句フィルタを残り6本にも当てて15本を現行既定で採り直した2026-09-03)）。
README・README_en・下の基準値表はすべて**現行既定の実測値**になっている。

**そのとき1つ結論が変わった。「15本すべてで silero 以上」は現行世代では成立しない。**
同世代で採り直すと **13本で12勝1敗**で、Summer Pockets #26 が 0.5pt 負ける
（**定型句フィルタが silero 側により多く効いたため順位が入れ替わった**）。
**群の合計ではどの群でも TEN の勝ちなので結論の向きは変わらない。**
README 2本から「15本すべて」を外した。

**(3) 未検証は残っていない。** `eval/score.py` の `dense` の例表示は
**通った**（[記録](MEASUREMENTS.md#scorepy-の-dense-例表示を動確した2026-09-03)）。
4種類すべてがタイムスタンプ付きで出て、**記録済みの `_eval/score-models5.txt` が
現行コードからバイト一致で再生成できる**ことも確認した。

**副産物で `eval/score.py` の CLI に不具合が2件出たが、着手不要。**
`--examples 0` の off-by-one と、`--ref` が既定の `sample` を置換せず追加すること。
**どちらも既定値では発火せず、記録された数値にも影響していない**
（[既知の問題 6](HANDOVER.md#6-evalscorepy-の-cli-に不具合が2件記録2026-09-03) に置いた）。

### 直近4回の作業（詳細は MEASUREMENTS）

| | 内容 | 記録 |
|---|------|------|
| **#24** | ボーカル抽出の不具合3件。**一度も正しく動いたことが無かった** | [#24](MEASUREMENTS.md#24-ボーカル抽出は3つの不具合で動いていなかった順序を直して測った2026-08-29) |
| **#25** | ニワトリ・ファイターは**言語自動判定の失敗**。`-l ja` で 115 → 315セグメント。**410秒は同時エンコードとの GPU 競合**で単体 89.7s | [#25](MEASUREMENTS.md#25-ニワトリファイターは言語自動判定の失敗だった-l-ja-で解決2026-09-02) |
| **#26** | **定型句フィルタを実装**（既定で有効）。上限測定と19本一致したので **ten 16.1% → 15.5% がそのまま実装の数値。** 参照15本で誤爆0件。**副産物で `--help` が exit 1 で死んでいたのを修正**（公開中の 0.9.2 でも死ぬ） | [#26](MEASUREMENTS.md#26-定型句フィルタを実装した20-の見送りを撤回2026-09-02) |
| **#27** | **モデル軸を閉じた。** 4候補すべてに large-v3 が勝ち 4.7〜18.2pt リード。**Whisper は32層が最大なので層数の軸に上は無い。** Omni-30B は 32GB に載らず、vLLM は Windows で import 不可。**事前変換済み CT2 が罠2 を100%踏むのを修正**。`eval/score.py` に `dense` 検出器を追加 | [#27](MEASUREMENTS.md#27-モデル軸を4候補で閉じたlarge-v3-が全勝2026-09-03) |

**この4回で[チェックリスト](HANDOVER.md#リリース前チェックリストexe-を作り直したら回す)が
14 → 16項目になった**（15 = `--help`、16 = `-m kotoba-v2`）。
**どちらも「README が案内している経路が一度も叩かれていなかった」型。**

**出力の回帰は確認済み**（16kHzモノラル・`-m tiny` で SRT/VTT の MD5 が記録値と一致）。
**既定の出力が変わるのは定型句が出る素材だけ。**

### 1.0 の条件は満たされた（2026-08-27 報告）

**0.9.2 が RTX 4080 と RTX 2070 の両方で、回避策なしに直接実行で動いた**
（[記録](MEASUREMENTS.md#決着092-が-4080-と-2070-の両方で回避策なしに動いた2026-08-27-報告)）。
**4080 は `CUDA_PATH` が CUDA 11 を指す環境**で、
[N](MEASUREMENTS.md#n-cuda_path-が同梱の-cublas-を隠していた2026-08-24-報告092-で修正) の
発火条件をそのまま満たす1台。**そこで `.bat` なしで動いたので、修正が実環境で効いた確認になっている。**
**Amatsukaze の紹介ページも exe 版の手順に書き換えられた**
（[sm_120 説の訂正](HANDOVER.md#訂正して伝える必要がある説明sm_120-フォールバック説)の反映も確認できた）。

**外部からの待ちは無くなった。** 残っているのは自分の手を動かす作業だけ。

**公開の確認は取れている。** リリース `v0.9.2` のアセット `whisp-carrier-0.9.2.7z`
（1,055,081,794 バイト）の digest が手元の SHA-256
`D6EEC1EC387821B9CDAB0726B5691863E714C65BB3DB050CCA6D96E51F51982F` と一致。

**0.9.2 の中身**（`CUDA_PATH` が同梱 cuBLAS を隠す不具合の2層修正、検証用の穴
`WHISP_CARRIER_CUDA_FIX`、回帰12項目）は
[N](MEASUREMENTS.md#n-cuda_path-が同梱の-cublas-を隠していた2026-08-24-報告092-で修正) に全部ある。
0.9.0 公開までの経緯は
[MEASUREMENTS.md の該当節](MEASUREMENTS.md#090-公開までの経緯2026-08-23-の記録)。
音声フィルターの結論は
[#23](MEASUREMENTS.md#23-音声フィルターを全部測った--ff_lowhighpass-だけが効く2026-08-25)
（`--ff_lowhighpass` だけが効き、残りは害。既定は変えない）。

### 現行の基準値（歌唱除外、指標は [#19](MEASUREMENTS.md#19-採点指標に欠陥があった参照キューの表示時間を発話時間として使っていた) 修正後）

**すべて現物の実測値で、すべて現行既定（TEN 0.75 + 定型句フィルタ）。**
出典は `_eval/score-fixed9.txt`（`ten-f1` 列）・`_eval/score-new4-f1.txt`・
`_eval/score-long-f1.txt`・`_eval/score-majilumiere-f1.txt`。

| 素材群 | 参照 | silero + フィルタ（旧既定） | **現行既定** | xxl |
|--------|------|------------------------|------------|-----|
| 24分もの9本 | 35,036字 | 19.0% | **15.5%** | 20.5% |
| 子供向け4本 | 16,704字 | 30.8% | **21.6%** | 33.5% |
| ハクメイ一挙 5h22m | 37,987字 | 未採り直し（フィルタなし 18.1%） | **15.6%** | 未測定 |
| マジルミエ一挙 4h52m | 50,255字 | 未採り直し（フィルタなし 16.0%） | **13.8%**（全測定中の最良） | 未測定 |

**定型句フィルタは15本中14本で改善、1本が同値、退行0本**
（[#28](MEASUREMENTS.md#28-定型句フィルタを残り6本にも当てて15本を現行既定で採り直した2026-09-03)）。
**カバレッジは4群すべてで不変で、動いたのは精度だけ** — 落としているのは
参照に無い文字だけという設計どおりの形。

**VAD の勝敗は同世代で 13本12勝1敗。** 負ける1本は Summer Pockets #26（0.5pt）。
**一挙2本の silero 側は同世代で採り直していない**ので、
**「15本全勝」とは書かないこと。**

**配布時に出す幅は「会話劇なら10%前後、子供向けでも20%台半ば、最悪でも30%未満」。**
旧既定では 30%超が4本あったが、現行既定では**1本も無い**（最悪が死亡遊戯 #09 の 28.8%）。

### 未決定のまま残したもの

**未決は無い。配布まで到達した。** 残っているのは外部の反応待ちだけ。

- **決定済み。`whisp-carrier.yaml` を配布物に入れるか → 入れない（`.example` だけ）。**
  **`自動` = turbo が分かった時点で判断の根拠が変わったが、結論は変わらなかった。**
  設定ファイルが守っていた値のうち `vad_method` はコード側の既定になり、
  beam 5 は欄を空にすれば入り、`language ja` は無影響。残るのは整形だけで、
  それは欄に `--standard_asia` と書けば済む。**唯一の反例が `自動` の打ち消し**
  （`model: large-v3` + `override: true`）で、これは `.example` に用途として書いた。
  開発機の `dist/whisp-carrier/whisp-carrier.yaml` は生きた設定として残す
- （**決定済み**）定型句フィルタは**実装した**（見送りを撤回・
  [#26](MEASUREMENTS.md#26-定型句フィルタを実装した20-の見送りを撤回2026-09-02)。
  既定で有効、`--filler_filter false` で切れる）、
  `--batched` は[追わない](HANDOVER.md#1---batched-は追わない判断記録2026-08-22)、
  turbo は[測らない](MEASUREMENTS.md#モデル欄の-自動-は-large-v3-turbo-になる)。
  いずれも再検討の条件を各節に書いてある

### git の状態

```
998798a (HEAD)           1.0.0 に上げ、15本を現行既定で採り直した ← 未 push・1.0.0 exe のコード状態
5b9ec12                  定型句フィルタと事前変換済みCT2（#24〜#28）← 未 push
f8c81fe                  音声フィルターを全部測って記録（#23）   ← 未 push
081b1d2                  音声フィルターの節を正確に書き直す      ← 未 push
0fed289                  リリース前チェックリストを文書化        ← 未 push
0336069                  1.0 の理由から --batched を外す        ← 未 push
4727b64                  引き継ぎ状態の整合を取る               ← 未 push
c89ea96                  sm_120 訂正が送付済みであることの記録   ← 未 push
2f39df8 (origin/main)    spec のコメント修正（exe に影響なし）   ← remote の v0.9.2 はここ
923016c                  DLL 探索の全点検の記録                ← 0.9.2 exe のコード状態
0b73544                  ← 0.9.1 の exe のコード状態
```

**未 push が8コミット。作業ツリーはクリーン。**
**配布する 1.0.0 exe は HEAD（`998798a`）そのものからビルドしてある** —
配布物と HEAD が一致しているのは版を上げた直後だけの状態で、
[通常は main が先に進む](HANDOVER.md#版番号とタグの運用スクリプト版だけの修正では版を上げない)。

**remote のタグ。** `v0.9.0` → `e165168` / `v0.9.1` → `0b73544` / `v0.9.2` → `2f39df8`。
**`v0.9.2` のローカルタグは `923016c` を指したままでずれている**ので、
`git tag -f v0.9.2 origin/main` で合わせる。
確認は `git fetch --tags` のあと `git ls-remote --tags origin`。

なお `.gitignore` は `_tmp*/`（ディレクトリ）だけを無視するので、
**`_tmp_*.ps1` のようなファイル名は引っかからない**（作業後に消すこと）。

### 次の1コマンド

**push してタグを打ち、リリースを作る。手元の成果物はできている。**

```powershell
# 1. 未 push 8コミット
git push origin main

# 2. 1.0.0 のタグ。main の HEAD に打つ
#    exe のコード状態は 998798a で、HEAD との差は記録の追記だけ。
#    HEAD に打つのは 0.9.2 でローカルと remote がずれたのと同じ轍を踏まないため
#    （gh release create はタグが無ければ既定ブランチの HEAD に作る）
git tag v1.0.0
git push origin v1.0.0

# 3. ずれている 0.9.2 のローカルタグを remote に合わせる（差分は spec のコメントだけ）
git tag -f v0.9.2 origin/main

# 4. リリースを作ってアセットを上げる。ノートはフル（数字が動くので）
gh release create v1.0.0 dist\whisp-carrier-1.0.0.7z --title "v1.0.0" --notes-file <ノート>

# 5. アップロード後にアセットの digest と照合する（チェックリスト項目14の後半）
#    6494C638419B76FC6A43A103C7EF3309C4F295646570850E167C09791226AC29
```

**リリースノートはフルにする。** 定型句フィルタで数字が動いたので
[書き方の分岐](HANDOVER.md#リリースノートの書き方2026-08-23-に決めた)の
「数字を動かす」側。**0.9.2 のノートに入れなかった英語節も、ここで戻す**
（1.0 は新規の読者が最初に着地する場所）。

**ノートに書く数字は README の「測定した精度」と同じもの**にすること。
24分もの9本 **15.5%**、子供向け4本 **21.6%**、一挙 **15.6%** と **13.8%**、
XXL r245.4 は 20.5% / 33.5%。**幅は 8.0〜28.8% で、30%超は1本も無い。**

**作業用に残したものの片付け**（`_eval/` は gitignore 済みなのでコミット対象にはならない）。

```powershell
Remove-Item _eval\_tmp_*, _eval\_ck -Recurse -Force -ErrorAction SilentlyContinue

# 原音セット2つ（約2.9GB + 約0.5GB）。#24 は測り終えたので消してよい。
# 作り直すなら手順は MEASUREMENTS の #24 にある
# Remove-Item -Recurse -Force _eval\wav48, _eval\wav48to16
```

**1.0 のノートで持ち越すもの。0.9.2 のノートには英語節が入っていない**
（0.9.0 / 0.9.1 にはある）。**1.0 は新規の読者が最初に着地する場所**なので、
そこでフルのノートを書くときに英語節を戻す。0.9.2 側は直さない
（[ノートは後から書き直さない](HANDOVER.md#リリースノートの書き方2026-08-23-に決めた)方針、
かつ 1.0 が出れば上に積まれる）。

### 公開したあと

| 状況 | やること |
|------|---------|
| **1.0.0 が問題なく使われている** | [L（回帰テストの穴）](#l-回帰テストの穴を塞ぐ)の残り＝**pytest 化**をやる。対象は環境に依存しない純粋関数だけ。**`model_dir=None` のケースを最初に入れる**（[1.0.0 の初回ビルドが落ちた原因](MEASUREMENTS.md#100-の初回ビルドが既定の実行で全部落ちたpathnone2026-09-03)） |
| **不具合の報告が来た** | [宣言した動作環境の内側で落ちる4件の表](HANDOVER.md#5-宣言した動作環境の内側で落ちる経路イシュー待ちにした2026-08-23)と突き合わせる。該当したら版を上げて作り直す |
| **Amatsukaze 側の記載で確認事項が来た** | 仕様は README と HANDOVER にある。**必要な情報は出すと伝えてある** |

**1.0.0 の exe 自体は第三者環境で未検証。** 第三者が確認したのは 0.9.2 で、
1.0.0 には #24〜#28 のコード変更が載っている。
**ただし DLL の解決経路（N の修正）は触っていない**ので、
**確認済みなのは「あの機構が他人の環境で効く」まで。**
**J・N・`Path(None)` の3件は全部「開発機に在るものが配布物の不備を隠す」形**だったので、
**次に疑うのもそこ。**

## 現状

**文字起こしの精度は本家 Faster-Whisper-XXL を上回り、exe は配布できる状態で、
Amatsukaze から実運用で24分の本編が通っている。**
**素材15本を現行既定で測り終え、精度側の測定は完了した**
（[0.9.0 公開までの経緯](MEASUREMENTS.md#090-公開までの経緯2026-08-23-の記録)、
[#28](MEASUREMENTS.md#28-定型句フィルタを残り6本にも当てて15本を現行既定で採り直した2026-09-03)）。
**VAD の勝敗は同世代で 13本12勝1敗**（以前の「15本全勝」は現行世代では成立しない）。

> **この節は修正後の指標（[#19](MEASUREMENTS.md#19-採点指標に欠陥があった参照キューの表示時間を発話時間として使っていた)）で採点し直した数値。**
> 歌唱除外あり。24分もの9本、参照 35,036字。生データは `_eval/score-fixed9.txt`。

| | 旧既定 silero + フィルタ | **現行既定**（TEN 0.75 + 定型句） | Faster-Whisper-XXL r245.4 |
|---|---|---|---|
| 全文CER（24分もの9本） | 19.0% | **15.5%** | 20.5% |
| per-block CER | 20.0% | **16.6%** | 24.3% |
| coverage | 82.6% | **86.6%** | 82.3% |
| precision | **91.6%** | 90.8% | 89.3% |
| ファイル別の勝敗（現行既定 対 旧既定） | — | **8勝1敗** | — |
| 30秒超セグメント | **0件** | **0件** | 9件 / 602s |
| thin セグメント | **0件** | 1件 | 15件 |
| ループ残存文字数 | 32字 | 35字 | 53字 |
| RTX 5090 で既定のまま動くか | **動く**（float16） | **動く** | 動かない（`-ct float32` が必須、float16の2倍遅い） |
| 速度（24分もの1本） | **85s** | 70〜100s | 175s |

**両列とも定型句フィルタ込みで揃えてある**（`clip-f1` と `ten-f1`）。
**exe はまだ作り直していないので、配布中の 0.9.2 はこの列ではない** —
0.9.2 は `ten-t075`（16.1%）の挙動。

**1敗は Summer Pockets #26（12.9% 対 13.4%）。**
フィルタなしでは同値だったが、**フィルタが silero 側により多く効いて順位が入れ替わった**
（[#28](MEASUREMENTS.md#28-定型句フィルタを残り6本にも当てて15本を現行既定で採り直した2026-09-03)）。

**ファイル別（全文CER）。`ten + 定型句` が現行既定の列。**

| 素材 | clip（既定） | ten 0.75 | ten + 定型句 | xxl |
|------|------------|----------|-------------|-----|
| 公女殿下 #10 | 8.8% | 8.7% | **8.0%** | 10.8% |
| LIAR GAME #10 | 11.1% | 9.7% | **9.7%** | 15.5% |
| 桃源暗鬼 | 12.6% | 11.9% | **11.6%** | 12.8% |
| Summer Pockets #26 | 13.8% | 13.8% | 13.4% | 15.1% |
| クレバテス #06 | 16.7% | 16.4% | **15.5%** | 18.2% |
| 終末ツーリング #12 | 17.3% | 14.0% | **13.6%** | 21.1% |
| ニンジャラ | 29.2% | 27.4% | **26.7%** | 30.3% |
| おねがいアイプリ | 40.9% | 23.7% | **22.9%** | 39.9% |
| 死亡遊戯 #09 | 42.4% | 30.5% | **28.8%** | 35.1% |

**TEN の効きは素材で桁違いに違う。** 会話劇では ±1.4pt の範囲にとどまり
（1本は 0.5pt の悪化）、**子供向け・低発話率の素材では 2〜21pt 改善する**
（アイプリ −18.0pt、死亡遊戯 −12.9pt。いずれも同世代＝両側フィルタ込みの差）。
silero が落としていた発話がそこに集中していたということ。

**代わりに払っているものが1つある。無字幕域への出力が 1775字 → 3686字（2.1倍）。**
CERには入らないが、字幕としては提供クレジット・番宣・主題歌にキューが付く方向。
[「歌唱・OP/EDは対象外」の位置づけ](MEASUREMENTS.md#歌唱oped-の扱い対象外だが拾う)を参照。

**精度は素材の内容で大きく変わる。単一の数字で語らないこと。**
15本を測った結果、番組の性質で3つの層に分かれた。

| 層 | CER全文 | coverage | 該当素材 |
|----|--------|----------|---------|
| **会話劇・職場もの** | **9〜18%** | 85〜93% | 公女殿下 8.8% / LIAR GAME 11.1% / 桃源暗鬼 12.6% / Summer Pockets 14.0% / **マジルミエ一挙 17.3%** |
| 一般（アクション・日常） | 17〜21% | 80〜86% | クレバテス 16.7% / 終末ツーリング 18.3% / ハクメイ一挙 20.5% |
| **子供向け番組** | **21〜54%** | 47〜80% | ニンジャラ 21〜32% / ぷにる 30.6% / プリキュア 34.8% / **アイプリ 54%** |

> **上の表は silero 前提の記録。TEN で子供向けの層が消えた。**
> 4本合計 31.1% → **21.9%**、カバレッジ 70.3% → **82.1%**
> （[追加4本](MEASUREMENTS.md#追加4本の-ten-測定2026-08-22決着)）。
> 「実装では直せない・XXL も同じ壁に当たる」という根拠は、
> **XXL も同じ silero を使うので同じ壁を2回測っていただけだった。**

**現行既定（TEN + 定型句）での層。** 15本すべて測り終えた結果、幅は
**8.0〜28.8%** に縮んだ（[#28](MEASUREMENTS.md#28-定型句フィルタを残り6本にも当てて15本を現行既定で採り直した2026-09-03)）。

| 層 | CER全文 | 該当素材 |
|----|--------|---------|
| **会話劇・職場もの** | **8.0〜13.8%** | 公女殿下 8.0% / LIAR GAME 9.7% / 桃源暗鬼 11.6% / Summer Pockets 13.4% / 終末ツーリング 13.6% / **マジルミエ一挙 13.8%** |
| 一般（アクション・日常） | 15〜16% | クレバテス 15.5% / **ハクメイ一挙 15.6%** / ニンジャラ「花火」16.0% |
| **子供向け番組** | **22〜25%** | プリキュア 22.6% / アイプリ 22.9〜23.4% / ぷにる 25.0% |
| 悪い側の2本 | 26.7〜28.8% | ニンジャラ「ガムッチ」26.7% / 死亡遊戯 #09 28.8%（発話率 0.23） |

24分もの9本の合計は **15.5%**、子供向け4本の合計は **21.6%**。
**配布時に出す幅は「会話劇なら10%前後、子供向けでも20%台半ば、最悪でも30%未満」。**
旧既定では 30%超が4本あったが、**現行既定では1本も無い。**
**ニンジャラは同じ番組で 16.0% と 26.7% に割れる**ので、層を番組名1つで語らないこと。
詳細は[測定結果 #5](MEASUREMENTS.md#5-cer-24-の読み方合計値に意味は薄い)と[#28](MEASUREMENTS.md#28-定型句フィルタを残り6本にも当てて15本を現行既定で採り直した2026-09-03)。

**長尺劣化は無い。** 一挙2本は現行既定で **5h22m が 15.6%、4h52m が 13.8%**。
後者は**全測定中の最良値**で、24分もの9本の合計（15.5%）より良い。30秒超は両方0件。
**ハクメイの thin 1件も定型句フィルタで消えて0件になった。**
VAD の所要は全体の 7〜9% で、総時間も silero と ±5%（[一挙2本](MEASUREMENTS.md#一挙2本の-ten-測定2026-08-22決着)）。

## 次の着手

**この節は開いているものだけを置く。** 終わったものは
[完了した着手の記録](MEASUREMENTS.md#完了した着手の記録fgiheacb-と運用メモ)へ移し、ここからは消す。
**A〜I の旧項目はすべて決着している**（F・E・I・H・A・B 完了、G 見送り、C 不要）。

> **ルール。** 揮発性の節（この文書のすべて）は**置換**し、
> 記録の節（[測定結果](MEASUREMENTS.md#測定結果)・[完了した着手の記録](MEASUREMENTS.md#完了した着手の記録fgiheacb-と運用メモ)・[作業記録](MEASUREMENTS.md#作業記録)）は**追記**する。
> 完了したら本文ごと記録側へ動かす。この節が669行あって中身が「着手すべきものは無い」
> だったのは、完了項目を取り消し線で残したまま積んでいたため。

| | 内容 | 状態 |
|---|------|------|
| ~~**J**~~ | ~~同梱 ffmpeg が使われていなかった~~ | **修正済み。** [記録](MEASUREMENTS.md#j-同梱-ffmpeg-が使われていなかった2026-08-23)。0.9.1 に入っている |
| ~~**K**~~ | ~~torch を外して配布サイズを削る~~ | **採用した。既定ビルドが slim になった。** [記録](MEASUREMENTS.md#k-torch-を外して配布サイズを削る2026-08-23)。payload 4780 → 2239 MB |
| **L** | [回帰テストの穴](#l-回帰テストの穴を塞ぐ) | **半分閉じた。** 手順は[リリース前チェックリスト](HANDOVER.md#リリース前チェックリストexe-を作り直したら回す)として文書化。**残りは pytest 化だけ** |
| ~~**M**~~ | ~~0.9.1 の再パックと公開~~ | **公開済み**（v0.9.1、2026-08-23） |
| ~~**N**~~ | ~~`CUDA_PATH` が同梱 cuBLAS を隠す~~ | **修正済み・回帰テスト済み。** [記録](MEASUREMENTS.md#n-cuda_path-が同梱の-cublas-を隠していた2026-08-24-報告092-で修正)。0.9.2 の exe はビルド済み、**再パックとリリースが残っている** |
| ~~**D**~~ | ~~[フィルター失敗の残り](MEASUREMENTS.md#d-フィルター失敗の残り優先度低)~~ | **閉じた。** 「`--ff_lowhighpass` で後半が消える」は**実素材10本で再現せず**（[#23](MEASUREMENTS.md#23-音声フィルターを全部測った--ff_lowhighpass-だけが効く2026-08-25)）。順序バグの修正で消えていた |
| ~~**O**~~ | ~~ボーカル抽出を10本に広げる~~ | **閉じた。** 10本で合計 +0.4pt・3勝2分5敗（[#24](MEASUREMENTS.md#24-ボーカル抽出は3つの不具合で動いていなかった順序を直して測った2026-08-29)）。**素材で振り分ける根拠も出なかった**ので、`--ff_vocal_extract` は「使わない」で確定 |
| ~~**P**~~ | ~~実運用で精度が崩れた1本~~（ニワトリ・ファイター第三羽） | **解析済み。原因は言語の自動判定で、`-l ja` で直る**（[#25](MEASUREMENTS.md#25-ニワトリファイターは言語自動判定の失敗だった-l-ja-で解決2026-09-02)）。0.9.2 で桁まで再現、`-l ja` で 115 → 315セグメント。**410秒は同時エンコードとの GPU 競合**で単体では 89.7s。緩和として低確信度の警告を1行入れた |
| ~~**Q**~~ | ~~「不可能なキュー密度」の検出器が無い~~ | **入れた。`eval/score.py` の参照不要指標が4つになった**（`loops` / `over30s` / `thin` / **`dense`**）。15字/秒超かつ20字以上。**手計算を再現**（第三羽 39/103・`-l ja` 2/311・コーパス 2/3,658）。**修復は入れない** — 原因が言語判定なので対象が無い。副産物として同じ4本で **XXL 8件 / kotoba-v2 7件（ピーク500字/秒）対 既定1件** が見え、**Qwen の per-block 47〜54% が粒度の問題だと切り分けられた**（[#27](MEASUREMENTS.md#dense-検出器を入れた参照不要指標が4つになった2026-09-03)）。レポートは `_eval/score-models5.txt` に反映済み |

### L. 回帰テストの穴を塞ぐ

> **半分閉じた（2026-08-25）。手順は
> [リリース前チェックリスト](HANDOVER.md#リリース前チェックリストexe-を作り直したら回す)として
> 文書化した**（14項目、うち5件が「環境を1つ壊して確認する」もの）。
> **残っているのは pytest 化だけで、対象は環境に依存しない純粋関数に絞る。**
> **CI にはしない**（GPU と 2.2GB の成果物が要り、exe を作り直す頻度が低いので
> 自動化したテスト自体が腐る）。以下は文書化前の記述で、理由の記録として残す。

**J を検出できなかった理由が2つあり、どちらも回帰テストの構造。**

- **`test_speech.wav` は 22050 Hz の WAV。** 非WAV入力の変換分岐を一度も通らないので、
  ffmpeg を起こす経路が未テストだった。**実運用の入力は AAC でそこを必ず通る**
- **この開発機は PATH に ffmpeg がある**（WinGet）。同梱版が使われていなくても動く

**最低限入れるべきは3ケース。** どれも既に手元で回した形がある。

| ケース | 検出できるもの |
|-------|--------------|
| AAC 入力 × PATH から ffmpeg を除去 | J とその再発 |
| 16 kHz モノラル WAV で SRT/VTT の MD5 照合 | 出力の回帰（現在は 22050 Hz で照合していて、リサンプラ差を巻き込む） |
| **`CUDA_PATH` を cublas 不在のディレクトリに向けて実行** | **[N](MEASUREMENTS.md#n-cuda_path-が同梱の-cublas-を隠していた2026-08-24-報告092-で修正) とその再発。** あわせて**ロードされた DLL のフルパス**を `Get-Process` の `.Modules` で確認し、`_internal` のものが読まれていることまで見る |

**あわせて pytest を入れる価値がある。** [検証済みのこと](MEASUREMENTS.md#検証済みのこと)に並んでいる
ケースは既に散文で書き終わっているので、写すだけで動く
（`sanitize_segments()` の12ケース、`loop_filter` の検出6件・保持9件、
`normalize.edit_distance_large` / `lcs_large` のランダム300件、`whisp_config` の型変換）。
**現状テストは0件で `.github/` にも workflow が無い。**

#### 手で回すなら（J の検証に実際に使った形）

`_tmp_*` に置いて使い捨てたので、必要になったら書き直すこと。
**要点は「PATH から ffmpeg を持つディレクトリだけをプロセス内で外す」**で、
マシンの PATH は触らない。

```powershell
# 1. 16kHz モノラルWAV の基準を作る（22050Hz の test_speech.wav をそのまま使わない）
& .\dist\whisp-carrier\_internal\ffmpeg.exe -y -i test_speech.wav `
      -ar 16000 -ac 1 -c:a pcm_s16le _tmp_gold16k.wav

# 2. Amatsukaze と同じ AAC を作る
& .\dist\whisp-carrier\_internal\ffmpeg.exe -y -i test_speech.wav -c:a aac -b:a 128k _tmp_probe.m4a

# 3. ffmpeg を持つ PATH エントリだけをこのプロセスから外す
$ffDirs = @(); foreach ($d in ($env:PATH -split ';')) {
    if ($d -and (Test-Path (Join-Path $d 'ffmpeg.exe'))) { $ffDirs += $d } }
$orig = $env:PATH
$env:PATH = (($orig -split ';') | Where-Object { $ffDirs -notcontains $_ }) -join ';'

# 4. AAC を処理できること（J の回帰）。exit 0 で字幕が出れば通る
& .\dist\whisp-carrier\whisp-carrier.exe _tmp_probe.m4a -m tiny -f srt --no_config --beep_off --output_dir .

$env:PATH = $orig

# 5. 出力の回帰。SRT/VTT の MD5 が記録値と一致すること
& .\dist\whisp-carrier\whisp-carrier.exe _tmp_gold16k.wav -m tiny -f srt vtt --no_config --beep_off --output_dir .
Get-FileHash _tmp_gold16k.srt, _tmp_gold16k.vtt -Algorithm MD5
#   SRT AE21834629D4762F0BCD2405E102D1DB
#   VTT 6A6791FD97DE025C91A202223185C868
```

> **ターミナルの出力が壊れる環境では、`.ps1` に書いて
> `powershell -File x.ps1 > x.log 2>&1` にしてからログを読むこと。**
> exe の出力を直接 `Start-Process -RedirectStandardOutput` でファイルに落とすのが
> いちばん確実で、パイプ越しだと途中で切れることがある（この検証中に何度も踏んだ）。

---
